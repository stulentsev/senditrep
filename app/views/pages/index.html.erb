<div class = 'window window-vpadding'>
  <h2>Все страницы</h2>

<% @pages.each do |page| %>
  <div>
      <div>
        <b><%= link_to page.title, page_path(page) %></b>
      </div>
      <div>
        <%= page.content.html_safe %>
      </div>
      <br/>
  </div>
<% end %>
</div>
